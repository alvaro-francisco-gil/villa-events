# Public

Next.js static directory — files here are served from the site root (e.g. `public/favicon.ico` → `/favicon.ico`).

Master copies of shared assets live in [`packages/shared/assets/`](../../../packages/shared/assets/). Copy build-time/web-served files (favicon, og:image, manifest icons) into this directory.
